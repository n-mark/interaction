apiVersion: v2
name: arch-homework
description: Auth + Profile + Billing + Order + Notification services behind Traefik with ForwardAuth on arch.homework
type: application
version: 0.2.0
appVersion: "0.2.0"