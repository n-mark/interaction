apiVersion: v1
kind: ConfigMap
metadata:
  name: billing-service-config
  namespace: {{ include "arch.namespace" . }}
data:
  BROKER_TYPE: "RABBITMQ"
  PG_HOST: "postgres-billing.{{ include "arch.namespace" . }}.svc.cluster.local"
  PG_PORT: "5432"
  PG_DATABASE: {{ .Values.postgresBilling.database | quote }}
  PG_SSLMODE: "disable"
  RABBIT_HOST: "rabbitmq.{{ include "arch.namespace" . }}.svc.cluster.local"
  RABBIT_PORT: {{ .Values.rabbitmq.amqpPort | quote }}
  APP_PORT: "{{ .Values.billing.port }}"
---
apiVersion: v1
kind: Secret
metadata:
  name: billing-service-secret
  namespace: {{ include "arch.namespace" . }}
type: Opaque
stringData:
  PG_USER: {{ .Values.postgresBilling.user | quote }}
  PG_PASSWORD: {{ .Values.postgresBilling.password | quote }}
  RABBIT_USERNAME: {{ .Values.rabbitmq.user | quote }}
  RABBIT_PASSWORD: {{ .Values.rabbitmq.password | quote }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: {{ include "arch.namespace" . }}
  labels:
    app: billing-service
spec:
  replicas: {{ .Values.billing.replicas }}
  selector:
    matchLabels:
      app: billing-service
  template:
    metadata:
      labels:
        app: billing-service
    spec:
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z postgres-billing.{{ include "arch.namespace" . }}.svc.cluster.local 5432; do
                echo "waiting for postgres-billing..."; sleep 2;
              done
          resources:
            requests: { cpu: "10m", memory: "16Mi" }
            limits:   { cpu: "50m", memory: "32Mi" }
        - name: wait-for-rabbitmq
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z rabbitmq.{{ include "arch.namespace" . }}.svc.cluster.local {{ .Values.rabbitmq.amqpPort }}; do
                echo "waiting for rabbitmq..."; sleep 2;
              done
          resources:
            requests: { cpu: "10m", memory: "16Mi" }
            limits:   { cpu: "50m", memory: "32Mi" }
      containers:
        - name: billing-service
          image: "{{ .Values.image.billing.repository }}:{{ .Values.image.billing.tag }}"
          imagePullPolicy: {{ .Values.image.billing.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.billing.port }}
          envFrom:
            - configMapRef:
                name: billing-service-config
            - secretRef:
                name: billing-service-secret
          # readinessProbe:
          #   httpGet: { path: /health, port: http }
          #   initialDelaySeconds: 5
          #   periodSeconds: 5
          # livenessProbe:
          #   httpGet: { path: /health, port: http }
          #   initialDelaySeconds: 15
          #   periodSeconds: 10
          resources:
{{ toYaml .Values.billing.resources | indent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: {{ include "arch.namespace" . }}
spec:
  selector:
    app: billing-service
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.billing.port }}
      protocol: TCP
